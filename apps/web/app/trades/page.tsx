import StagePlaceholder from "@/components/StagePlaceholder";

export default function TradesPage() {
  return (
    <StagePlaceholder
      stage="Stage 3"
      title="Trade rumors"
      description="A rumor feed pulled from ESPN and Reddit, scored by source credibility (insider vs. beat reporter vs. aggregator)."
    />
  );
}
