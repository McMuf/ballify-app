import StagePlaceholder from "@/components/StagePlaceholder";

export default function BacktestPage() {
  return (
    <StagePlaceholder
      stage="Stage 7"
      title="Backtesting"
      description="How sentiment-implied favorites have tracked against actual game outcomes over time — this is what earns trust in the signal."
    />
  );
}
