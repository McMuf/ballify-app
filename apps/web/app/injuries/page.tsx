import StagePlaceholder from "@/components/StagePlaceholder";

export default function InjuriesPage() {
  return (
    <StagePlaceholder
      stage="Stage 3"
      title="Injury report"
      description="Live injury statuses with a modeled impact on each team's win probability."
    />
  );
}
